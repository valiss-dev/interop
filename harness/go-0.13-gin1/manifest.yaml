id: go-0.13-gin1
library: valiss-go
adapter: gin
adapter_version: "1"
version: "0.13"
spec: [1]

server:
  transports: [http]
  modes: [signed, message]
  features:
    allowlist: true
    replay: true
    epoch: true
    bearer: true

issue: [operator, account, user, message]
build:
  dockerfile: Dockerfile
