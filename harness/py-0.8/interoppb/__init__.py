"""The interop harness gRPC surface (valiss.interop.v1.Interop), defined by
interop.proto — a copy of the go-0.12 reference entry's, which defines the
service every gRPC-capable entry implements. The interop_pb2 and
interop_pb2_grpc modules are generated from it by grpcio-tools during the
image build (see the Dockerfile); they are not committed."""
