# go build output of the orchestrator module
/orchestrator/orchestrator

# go build output of the fixture generator module
/fixture/gen/gen

# machine report written by the CI workflow / local runs
matrix-report.json

# local Claude Code state (worktrees, session files)
.claude/
