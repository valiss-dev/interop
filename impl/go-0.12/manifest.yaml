id: go-0.12
library: valiss-go
version: "0.12"
spec: [1]

server:
  transports: [http, grpc]
  modes: [signed, message]
  features:
    allowlist: true
    replay: true
    epoch: true
    bearer: true
client:
  transports: [http, grpc]
  modes: [signed, message]

issue: [operator, account, user, message]
build:
  dockerfile: Dockerfile